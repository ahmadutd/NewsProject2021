@using AllNews.Domain;
@model IEnumerable<Language>

@foreach (var item in Model)
{

    <a href="/Home/SetLanguage/@item.LangTitle"><span class="fas fa-language mr-1 ml-1"></span>@item.LangName</a>//controller/action/routeva

}
