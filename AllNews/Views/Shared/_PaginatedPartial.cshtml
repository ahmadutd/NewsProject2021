@using Microsoft.AspNetCore.Mvc.Localization;
@using System.Globalization;
@inject IViewLocalizer Localizer
@*<nav aria-label="...">
        <ul class="pagination pagination-sm">
            <li class="page-item @(Model.CurrentPage <= 1?"disabled":"")">
                <a href="/WorldNews?page=1" class="page-link">@Localizer["First"]</a>
            </li>

            <li class="page-item @(Model.CurrentPage <= 1?"disabled":"")">
                <a href="/WorldNews?page=@(Model.CurrentPage-1)" class="page-link">@Localizer["Preview"]</a>
            </li>

            @for (int i = 1; i <= Model.TotalPage; i++)
            {
                <li class="page-item @(i==Model.CurrentPage?"active":"")">
                    <a href="/WorldNews?page=@i" class="page-link">@i</a>
                </li>
            }

            <li class="page-item @(Model.CurrentPage >= Model.TotalPage?"disabled":"")">
                <a href="/WorldNews?page=@(Model.CurrentPage+1)" class="page-link">@Localizer["Next"]</a>
            </li>

            <li class="page-item @(Model.CurrentPage >= Model.TotalPage?"disabled":"")">
                <a href="/WorldNews?page=@(Model.TotalPage)" class="page-link">@Localizer["Last"]</a>
            </li>
        </ul>
    </nav>

<div class="pagination pagination-sm">
    
    <a href="/WorldNews?page=1" class="@(Model.CurrentPage <= 1?"disabled":"")">@Localizer["First"]</a>
    
    @for (int i = 1; i <= Model.TotalPage; i++)
     {
    
        <a href="/WorldNews?page=@i" class="@(i==Model.CurrentPage?"active":"")">@i</a>
    
     }
     
    <a href="/WorldNews?page=@(Model.TotalPage)" class="@(Model.CurrentPage >= Model.TotalPage?"disabled":"")">@Localizer["Last"]</a>
            
            
</div>*@

